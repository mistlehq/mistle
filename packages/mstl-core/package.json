{
  "name": "@mistle/mstl-core",
  "private": true,
  "scripts": {
    "build": "go test ./... -run '^$'",
    "lint": "go vet ./...",
    "typecheck": "go test ./... -run '^$'",
    "test": "go test ./...",
    "format": "gofmt -w .",
    "format:check": "test -z \"$(gofmt -l .)\""
  }
}
