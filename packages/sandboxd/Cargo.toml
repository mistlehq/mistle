[package]
name = "sandboxd"
version = "0.0.0"
edition = "2024"

[dependencies]
nix = { version = "0.30.1", features = ["process", "signal", "socket", "user"] }
serde = { version = "1.0.228", features = ["derive"] }
serde_json = "1.0.149"
