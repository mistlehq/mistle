# Data-Plane Workflow Library

Reference catalog of data-plane workflows in `@mistle/workflows`.

## Workflows

No data-plane workflows are registered yet.

When the first workflow is added, document:

- workflow purpose
- exported spec name
- workflow name/id
- input and output contract
- any runtime dependencies required for registration
