[package]
name = "commit-sign"
version = "0.0.0"
edition = "2024"

[dependencies]
base64 = "0.22.1"
serde = { version = "1.0.228", features = ["derive"] }
serde_json = "1.0.149"
ssh-key = { version = "0.7.0-rc.10", features = ["crypto", "std"] }
