{
  "name": "@mistle/data-plane-worker",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "concurrently --kill-others --names worker,reaper \"tsx watch --import ./instrument.ts ./node_modules/@openworkflow/cli/dist/cli.js worker start --config ./openworkflow.config.ts\" \"pnpm run dev:reaper\"",
    "dev:reaper": "tsx watch --import ./instrument.ts ./reaper/index.ts",
    "build": "tsc -p tsconfig.build.json && tsc-alias -p tsconfig.build.json && tsc -p tsconfig.build.openworkflow.json && tsc-alias -p tsconfig.build.openworkflow.json",
    "start": "node --import ./dist/instrument.js ./node_modules/@openworkflow/cli/dist/cli.js worker start --config ./dist/openworkflow.config.js",
    "start:reaper": "node --import ./dist/instrument.js ./dist/reaper/index.js",
    "lint": "oxlint --type-aware --deny-warnings -c ../../.oxlintrc.jsonc .",
    "typecheck": "tsgo --noEmit",
    "test": "vitest run --passWithNoTests",
    "test:property": "vitest run --passWithNoTests",
    "format": "oxfmt .",
    "format:check": "oxfmt --check ."
  },
  "dependencies": {
    "@mistle/config": "workspace:*",
    "@mistle/db": "workspace:*",
    "@mistle/gateway-tunnel-auth": "workspace:*",
    "@mistle/logging": "workspace:*",
    "@mistle/sandbox": "workspace:*",
    "@mistle/telemetry": "workspace:*",
    "@mistle/time": "workspace:*",
    "@mistle/workflow-registry": "workspace:*",
    "@openworkflow/cli": "^0.4.3",
    "drizzle-orm": "^0.45.1",
    "openworkflow": "^0.8.1",
    "pg": "^8.20.0",
    "typeid-js": "^1.2.0",
    "zod": "^4.3.6"
  },
  "devDependencies": {
    "@mistle/test-harness": "workspace:*",
    "@types/pg": "^8.18.0",
    "concurrently": "^9.2.1"
  }
}
