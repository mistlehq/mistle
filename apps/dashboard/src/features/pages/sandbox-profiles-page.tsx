import { PagePlaceholder } from "./page-placeholder.js";

export function SandboxProfilesPage(): React.JSX.Element {
  return (
    <PagePlaceholder description="Browse and manage sandbox profiles." title="Sandbox profiles" />
  );
}
