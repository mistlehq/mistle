import { PagePlaceholder } from "./page-placeholder.js";

export function HomePage(): React.JSX.Element {
  return (
    <PagePlaceholder
      description="Overview landing page for authenticated dashboard users."
      title="Home"
    />
  );
}
