// Existing static table usages in runtime code. Do not add entries here for new code.
// Remove entries as nearby code is migrated to request/environment-bound schema tables.
export const StaticDbTableUsageBaseline = new Set([]);
